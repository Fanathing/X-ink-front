import styled from 'styled-components';

const StyledLoginButton = styled.button``;

const LoginButton = () => {
  return <StyledLoginButton>로그인 버튼</StyledLoginButton>;
};

export default LoginButton;
