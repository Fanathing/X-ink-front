import styled from 'styled-components';

const StyledCreateUserButton = styled.button``;

const CreateUserButton = () => {
  return <StyledCreateUserButton>회원가입 버튼</StyledCreateUserButton>;
};

export default CreateUserButton;
